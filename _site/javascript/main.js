$("document").ready(function(){



$("#typed").typed( {
   strings: ["Get Active.", "Eat Local.", "Give Back."],
   typeSpeed: 50,
   backDelay: 1000,
   loop: true,
   // defaults to false for infinite loop
   loopCount: false,
   });

 });
